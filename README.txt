

To allow the porgram to run within command line, within the directory of where the script.js file is located, please run the following command:

chmod u+x script.js
After that you will be able to run the command as follows:

node script.js "HH:MM" <config.txt
where HH:MM is the current time in 24h format ( e.g 14:30).